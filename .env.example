# Runtime configuration — copy to .env and adjust as needed.
# AWS credentials are resolved from the standard chain:
#   1. IAM role (recommended for Lambda / EC2)
#   2. aws configure / ~/.aws/credentials
#   3. Environment variables below

AWS_REGION=us-east-1
S3_BUCKET_NAME=cloudarchitectai-results-dev
